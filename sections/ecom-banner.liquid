<section id="ecom-banner-{{ section.id }}" class="homepage-banner">
  
  <!-- Top Header Bar -->
  <div class="banner-topbar">
    <div class="topbar-left">TISSO VISON</div>
    <div class="topbar-center">Find the ideal gift for your loved ones.</div>
    <div class="topbar-right">
      <a href="/collections/all" class="btn-yellow">CHOOSE GIFT →</a>
    </div>
  </div>

  <!-- Hero Banner Section -->
  <div class="banner-hero">
    <div class="banner-content">
      <h1 class="hero-heading">The Gift Guide</h1>
      <p class="hero-text">
        Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated 
        selection and find the perfect gifts to delight your loved ones this holiday season.
      </p>
      <a href="/collections/all" class="btn-black">SHOP NOW →</a>
    </div>
  </div>

  <!-- Footer Strip -->
  <div class="banner-footer">
    SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL
  </div>

</section>

<style>
/* -------- TOP HEADER BAR -------- */
.banner-topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 30px;
  font-size: 1rem;
  background: #fff;
  border-bottom: 1px solid #eee;
}
.topbar-left { font-weight: bold; font-size: 1.2rem; }
.topbar-center { text-align: center; flex: 1; color: #333; }
.topbar-right { text-align: right; }

/* Buttons */
.btn-yellow {
  background: #FFD700;
  color: #000;
  padding: 10px 18px;
  border-radius: 4px;
  font-weight: bold;
  text-decoration: none;
}
.btn-yellow:hover { background: #e6c200; }
.btn-black {
  background: #000;
  color: #fff;
  padding: 12px 24px;
  border-radius: 4px;
  font-weight: bold;
  text-decoration: none;
  display: inline-block;
  margin-top: 20px;
}
.btn-black:hover { background: #333; }

/* -------- HERO SECTION -------- */
.banner-hero {
  background: url('{{ "4.webp" | file_url }}') center/cover no-repeat; 
  text-align: center;
  padding: 80px 20px;
}
.banner-content {
  max-width: 800px;
  margin: 0 auto;
}
.hero-heading {
  font-size: 3rem;
  font-weight: bold;
  margin-bottom: 15px;
  color: #000;
}
.hero-text {
  font-size: 1.2rem;
  line-height: 1.5;
  color: #444;
}

/* -------- FOOTER STRIP -------- */
.banner-footer {
  text-align: center;
  padding: 10px;
  color: red;
  font-weight: bold;
  font-size: 0.9rem;
}

/* -------- RESPONSIVE -------- */
@media (max-width: 768px) {
  .banner-topbar {
    flex-direction: column;
    gap: 10px;
    text-align: center;
  }
  .hero-heading { font-size: 2rem; }
  .hero-text { font-size: 1rem; }
}
</style>
